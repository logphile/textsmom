{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.18.0"
  },
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "chmod +x node_modules/.bin/nuxt && nuxt prepare"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.52.1",
    "nodemailer": "^7.0.5",
    "nuxt": "^4.0.1",
    "vue": "^3.5.17",
    "vue-router": "^4.5.1"
  }
}
